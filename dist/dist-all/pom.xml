<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>dist-parent</artifactId>
    <groupId>com.intel.analytics.bigdl.core</groupId>
    <version>0.9.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.intel.analytics.bigdl.core.dist</groupId>
  <artifactId>all</artifactId>
  <packaging>jar</packaging>

  <properties>
    <sources-dir>${project.build.directory}/sources</sources-dir>
  </properties>

  <profiles>
    <profile>
      <id>linux</id>
      <dependencies>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>linux64</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
      </dependencies>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
    </profile>

    <profile>
      <id>win64</id>
      <dependencies>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>win64</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
      </dependencies>
    </profile>

    <profile>
      <id>mac</id>
      <dependencies>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>mac</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
      </dependencies>
    </profile>

    <profile>
      <id>rh5</id>
    </profile>

    <profile>
      <id>deploy</id>
      <dependencies>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>linux64</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>win64</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
        <dependency>
          <groupId>com.intel.analytics.bigdl.core.dist</groupId>
          <artifactId>mac</artifactId>
          <version>${project.version}</version>
          <type>pom</type>
        </dependency>
      </dependencies>
    </profile>
  </profiles>

  <build>
    <plugins>
      <!-- we should copy the sources, which will be used to generate the sources
           jar and javadoc jar. Each jar has only the files of BigDL-core. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>2.8</version>
        <executions>
          <execution>
            <id>unpack-dependency-sources</id>
            <phase>package</phase>
            <goals>
              <goal>unpack-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${sources-dir}</outputDirectory>
              <prependGroupId>true</prependGroupId>
              <includes>com/intel/**</includes>
              <excludes>**/*.xml</excludes>
              <classifier>sources</classifier>
              <excludeTypes>pom</excludeTypes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.1.0</version>
        <configuration>
          <filters>
            <!-- windows has this jar for passing building.
                 we don't have to need this in our all jar. -->
            <filter>
              <artifact>*:*</artifact>
              <excludes>
                <exclude>opencv-320.jar</exclude>
              </excludes>
            </filter>
          </filters>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet> 
                <includes>
                  <include>com.intel.analytics.bigdl.core*:*</include>
                </includes>
              </artifactSet> 
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- generate sources.jar -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>1.9</version>
        <executions>
          <execution>
            <id>add-source</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${sources-dir}</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.0.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <phase>package</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!-- generate javadoc.jar -->
      <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>2.10.1</version>
        <executions>
          <execution>
            <id>javadoc-jar</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <sourcepath>${sources-dir}</sourcepath>
              <detectOfflineLinks>false</detectOfflineLinks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
